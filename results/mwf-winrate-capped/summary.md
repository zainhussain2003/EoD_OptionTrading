I've written the complete `results/mwf-winrate-capped/summary.md`. It replaces the stale deterministic FAILURE summary (which reflected an earlier OpenBLAS crash, now fixed per commit `68d00a0`) with the success analysis for this run: With-Outliers Calls/Puts table, a note that no `outliers_removed` object is present, and the Oracle's Verdict flagging that ~half the sample is Black-Scholes simulated (Wednesday) — so the result is inconclusive until re-run on fully real option bars.
